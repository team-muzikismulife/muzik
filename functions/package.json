{
  "name": "muzik-functions",
  "version": "0.1.0",
  "private": true,
  "description": "MUZIK IS MY LIFE — Cloud Functions (백엔드설계.md §3)",
  "main": "lib/functions/src/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only auth,firestore,functions",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.1.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "~5.8.3"
  }
}
